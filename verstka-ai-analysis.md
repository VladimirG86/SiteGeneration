# VERSTKA.ai: технический разбор и план создания своего аналога

*Анализ проведён 22.09.2026 по открытому коду сайта, HTTP-заголовкам и примерам сгенерированных сайтов.*

---

## Часть 1. Как устроен VERSTKA.ai (факты из кода)

### 1.1. Сама платформа (verstka.ai)

| Компонент | Что найдено |
|---|---|
| Бэкенд | **Python + Django** (сервер gunicorn, CSRF-токен `__Secure-csrftoken`, серверный рендер HTML без SPA-фреймворков) |
| Фронт лендинга/кабинета | Собственные CSS/JS, иконки Lucide, минимальные зависимости — сайт быстрый и лёгкий (~96 КБ) |
| Хранилище | S3-совместимое облако **Cloud.ru** (`verstka-public.s3.cloud.ru`, `verstka-sites.s3.cloud.ru`) |
| Аналитика | Яндекс.Метрика (вебвизор, e-commerce), ВК-пиксель |
| Оплаты | Т-Банк, Robokassa (видно в CSP-заголовке: `pay.tbank.ru`, `auth.robokassa.ru`) |
| Капча | Яндекс SmartCaptcha |
| Хостинг | Россия, nginx + gunicorn, HSTS, строгий CSP |

### 1.2. Сайты, которые генерирует сервис

Проверено на живых примерах (s1637, s1639, s501.verstka.site):

- **Чистый статический HTML** (~75 КБ, 7 секций), лежащий в S3-хранилище как обычный файл — отдаётся через CDN с заголовками `x-amz-*`. **Никакого WordPress, Tilda или «конструктора» под капотом нет.**
- Стили — **Tailwind CSS**, причём для каждого сайта компилируется свой файл (~41 КБ, только используемые классы; md5 у всех сайтов разный). Это значит: ИИ пишет HTML с классами Tailwind → пайплайн собирает CSS.
- Шрифты — Google Fonts: Inter, Golos Text, Unbounded.
- Немного инлайнового **ванильного JavaScript** (слайдеры, галереи, лайтбокс) — без jQuery и тяжёлых библиотек.
- Медиа: изображения (webp) и даже **hero-видео (mp4)** хранятся в их S3 — генерируются/подбираются ИИ.
- Полный SEO-набор: мета-теги, canonical, Open Graph, JSON-LD разметка (Organization, WebSite, WebPage).
- Формы заявок уходят на `api.verstka.ai` (их Django-бэкенд), защита — SmartCaptcha.

### 1.3. Продуктовая механика и экономика

- Онбординг: **5 вопросов о бизнесе** → из ответов собирается промпт для ИИ.
- Генерация за несколько минут: структура + тексты + дизайн + изображения + видео.
- **ИИ-редактор**: правки через чат («сделай фон темнее», «добавь блок с ценами»).
- Ключ к экономике виден в тарифах: лимиты **400 / 800 / 2000 ИИ-правок в месяц** (Старт 990 ₽, Про 1 990 ₽, Бизнес 4 990 ₽). Правки дёшевы для платформы, но лимитируются, чтобы абьюзили модель.
- Встроенная мини-CRM, отправка заявок в Telegram/Max/email/AmoCRM/Битрикс24/webhook, свой домен + SSL, ежедневные бэкапы.

### 1.4. Главная инженерная идея (вывод)

VERSTKA.ai — это не «ИИ пишет произвольный сайт». Это **ИИ + жёсткая дизайн-система**: модель наполняет контентом заранее спроектированные типовые секции (hero, услуги, цены, отзывы, FAQ, контакты) в рамках Tailwind-палитры и готовых паттернов. Поэтому результаты стабильны и аккуратны. Именно на этом держится качество — и именно это вам придётся строить в первую очередь.

---

## Часть 2. Архитектура вашего аналога

```
┌─────────────────────────────── ВЕБ-ПЛАТФОРМА ───────────────────────────────┐
│  Лендинг · Регистрация · Личный кабинет · Чат-редактор · Мини-CRM · Биллинг │
└──────────────┬──────────────────────────────────────────────┬───────────────┘
               │                                              │
      ┌────────▼─────────┐                          ┌─────────▼─────────┐
      │  ОРКЕСТРАТОР     │   очередь задач          │  API ФОРМ/ЗАЯВОК  │
      │  генерации       │──────────────►           │  + анти-спам      │
      └────────┬─────────┘                          └─────────┬─────────┘
               │                                              │
   ┌───────────┼───────────────┬─────────────────┐            ▼
   ▼           ▼               ▼                 ▼       Telegram / email /
┌───────┐ ┌─────────┐  ┌──────────────┐  ┌──────────────┐ AmoCRM / Б24 / webhook
│ LLM   │ │ Генерация│  │ Генерация    │  │ Компиляция  │
│ тексты│ │ картинок │  │ видео/лого   │  │ Tailwind CSS│
│ код   │ │ (Flux/…) │  │ (Kling/стоки)│  │ + сборка    │
└───────┘ └─────────┘  └──────────────┘  └──────┬───────┘
                                                ▼
                              ┌──────────────────────────────┐
                              │ S3 + CDN: статика сайтов     │
                              │ *.yourdomain.ru (поддомены)  │
                              │ свои домены + авто-SSL (ACME)│
                              └──────────────────────────────┘
```

### Компоненты по слоям

**1. Ядро генерации (самое важное):**
- Подход A — LLM генерирует весь HTML+Tailwind сразу (так работает Verstka). Гибко, но требует сильной модели и хорошего промпта.
- Подход B — LLM генерирует структурированный JSON (секции, тексты, настройки темы), а рендер в HTML делает ваш код по библиотеке шаблонов. Дешевле, стабильнее, проще чинить, но меньше разнообразия.
- **Рекомендация: гибрид.** LLM выбирает и наполняет секции из контролируемой библиотеки паттернов + выбирает тему/палитру/шрифты. Картинки и видео генерируются отдельно параллельно.
- Модели (цены API, сент. 2026): Claude Sonnet ~$3/$15 за 1М токенов (вход/выход), GPT-5 ~$1.25/$10, DeepSeek ~$0.30/$1.20 (в 10–25 раз дешевле). Практика: черновая генерация на дешёвой модели, финальные правки и сложные запросы — на сильной.

**2. Генерация медиа:**
- Изображения: Flux, SDXL, Recraft (или российские Kandinsky, если важно 152-ФЗ). Либо генерация + стоки (Pexels/Unsplash API) для экономии.
- Видео: для hero — короткие генерации (Kling, Runway, Pixverse) либо лицензионные стоки с подбором по смыслу через LLM.
- Логотип/фавикон: генерация SVG-инициалов (как у Verstka — монограммы «АП», «ДЛ»).

**3. Пайплайн публикации:**
- Очередь задач (Celery + Redis / RabbitMQ), статус-машина генерации с прогрессом в UI.
- Сборка: HTML → компиляция Tailwind → минификация → загрузка в S3 → CDN.
- Поддомены `*.vashservice.ru` (wildcard DNS) для бесплатных сайтов.
- **Свои домены + SSL:** приём CNAME, проверка владения, автоматический выпуск сертификатов Let's Encrypt (Caddy on-demand TLS или certbot + ACME-хук) — это стандартный, решаемый узел.

**4. ИИ-чат-редактор:**
- Агент с инструментами: «прочитать секцию», «изменить текст/стиль/порядок», «добавить/удалить блок», «перегенерировать картинку».
- Правки по одной секции, а не всего сайта — так в разы дешевле и стабильнее (поэтому и лимиты в сотни правок реальны).
- История версий + откат (бэкапы).

**5. Приём заявок (мини-CRM):**
- API приёма форм с сайтов → БД → кабинет владельца.
- Уведомления: email (SMTP), Telegram Bot API, webhook-и, интеграции AmoCRM/Битрикс24 (оба имеют публичные REST API).
- Анти-спам: SmartCaptcha/hCaptcha + rate-limit + honeypot.

**6. Биллинг:** ЮKassa / Т-Банк / Robokassa, тарифы, пробный период («7 дней за 1 ₽» — классическая механика), учёт лимитов генераций/правок.

**7. Админка и безопасность:** модерация сгенерированных сайтов (обязательно — генераторы сайтов используют для фишинга), анти-абьюз, логи, метрики качества генерации.

---

## Часть 3. Рекомендуемый стек

### Сценарий А: рынок РФ (как Verstka)

| Слой | Технологии |
|---|---|
| Бэкенд | Python: **Django** (админка и CRM из коробки) или FastAPI; Celery для очередей |
| БД / кэш | PostgreSQL + Redis |
| Фронт кабинета | Django-шаблоны + Alpine.js/htmx (как сам Verstka) или Next.js |
| LLM | DeepSeek/Qwen (дёшево, массово) + Claude Sonnet / GPT-5 (сложные правки); опционально YandexGPT/GigaChat для полного соответствия 152-ФЗ |
| Изображения | Flux (API Replicate/fal.ai), SDXL на своём GPU при объёмах; Kandinsky (Сбер) для РФ |
| Видео | Kling/Runway API или лицензионные стоки |
| Хостинг статики | Cloud.ru / Yandex Object Storage + CDN |
| SSL/домены | Caddy (on-demand TLS) или certbot + ACME |
| Платежи | ЮKassa / Т-Банк / Robokassa |
| Инфраструктура | 2–3 VPS + Docker, на старте ~10–30 тыс. ₽/мес |

### Сценарий Б: глобальный рынок
Next.js + Supabase (Postgres/Auth) + Vercel/AWS + Cloudflare R2/CDN + Stripe + Claude/GPT API + Replicate для медиа.

---

## Часть 4. Себестоимость одной генерации (сентябрь 2026)

Статьи расходов на **один сайт**:

| Статья | Эконом-вариант (DeepSeek + Flux + стоки) | Премиум (Claude/GPT + Flux + видео-генерация) |
|---|---|---|
| Генерация HTML/текстов (~5–10К вход + 15–25К выход токенов с итерациями) | $0.02–0.06 | $0.4–0.8 |
| Изображения: 5–15 шт. | $0.1–0.5 | $0.5–1.5 |
| Видео для hero | 0 (сток) | $0.5–2 |
| **Итого за сайт** | **~$0.15–0.6 (15–60 ₽)** | **~$1.5–4 (150–400 ₽)** |
| ИИ-правка (по секции, малый контекст) | < $0.01 | $0.01–0.05 |

Вывод: себестоимость мизерная относительно тарифа 990 ₽/мес — бизнес-модель Verstka сходится легко. Основные затраты — **разработка и доведение качества генерации**, а не токены.

---

## Часть 5. Команда, сроки, бюджет (оценка)

| Этап | Состав | Срок | Ориентировочный бюджет |
|---|---|---|---|
| **MVP**: анкета → генерация → поддомен → чат-редактор → формы на email/Telegram | 1–2 разработчика (fullstack + кто-то с опытом LLM) | 2–4 мес | 1,5–4 млн ₽ |
| **Продукт уровня Verstka**: свои домены+SSL, биллинг, мини-CRM, интеграции с amoCRM/Б24, качество генерации | 3–5 человек (2–3 бэкенд, 1 фронт, 1 ML/промпт-инженер, дизайнер частично) | 6–12 мес | 10–30 млн ₽ |
| Переменные расходы | LLM + медиа + инфраструктура | — | от ~30–50 ₽ на сайт (эконом) до 400 ₽ (премиум) |

---

## Часть 6. Честные сложности и риски

1. **Качество генерации — это и есть продукт.** Первый промпт не даст уровень Verstka: нужны недели итераций над промптами, библиотека секций, дизайн-система, автотесты (скриншот-сравнение, оценка LLM-судьёй). Закладывайте 50–60% усилий именно сюда.
2. **Конкуренция.** В РФ: VERSTKA.ai, Craftum, Tilda (добавляет AI), Сайты от Мастера/Reg.ru и др. Глобально: Lovable, Bolt.new, v0, Framer AI, Durable, 10Web, Hostinger AI. **Нужна ниша/УТП**: вертикаль (стоматологии, автосервисы, недвижимость…), локальные интеграции (MAX, Яндекс.Бизнес), цена, скорость, SEO-упор.
3. **Юридика (РФ):** 152-ФЗ — персональные данные из форм должны храниться в РФ (LLM зарубежного провайдера для чата правок — отдельный вопрос, данные сайта с ПД туда отправлять нельзя без проработки); оферта; модерация контента пользователей; право на AI-изображения.
4. **Абьюз:** генераторы сайтов обожают фишеры. Нужны модерация, лимиты бесплатного тарифа, жалобы и блокировки — иначе домен вашей платформы попадёт под санкции почтовиков и поисковиков.
5. **Latency:** «сайт за 2 минуты» требует конвейера: генерация секций параллельно, стриминг прогресса, медиа — вторым этапом (сайт показывается сразу, картинки досоздаются).

---

## Часть 7. Пошаговый план запуска

**Фаза 0 — доказательство качества (1–2 недели, до всей команды).**
Скрипт: анкета (5 вопросов) → промпт → Claude/DeepSeek генерирует HTML+Tailwind по вашей библиотеке секций → сохранить HTML → открыть локально. Прогнать 20 разных бизнесов. Если качество после 10–15 итераций промпта приемлемо — проект жизнеспособен. Это стоит почти ничего и убивает/спасает идею до больших вложений.

**Фаза 1 — MVP (4–8 недель).**
Регистрация и кабинет, очередь генерации со стримингом статуса, публикация на `*.vashservice.ru`, чат-редактор (правки секций), формы заявок → email + Telegram. Дизайн-система: 3–5 тем, ~10 типов секций.

**Фаза 2 — монетизация (4–6 недель).**
Свои домены + авто-SSL, биллинг (ЮKassa/Т-Банк), тарифы и лимиты, Яндекс.Метрика, SEO-настройки (meta, robots, sitemap).

**Фаза 3 — удержание (1–2 мес).**
Мини-CRM в кабинете, AmoCRM/Битрикс24/webhooks, SmartCaptcha, бэкапы версий, галерея примеров, реферальная механика.

**Фаза 4 — рост (постоянно).**
Пайплайн оценки качества генераций (автоскриншоты + LLM-судья + метрики конверсии форм), A/B моделей и промптов, новые вертикали и шаблоны, собственный домен-зона для бесплатных сайтов, API для агентств (тариф «Бизнес»).

---

## Резюме

- **Да, воспроизвести возможно** — ни какой-либо секретной технологии, ни экзотического стека здесь нет: Django/Python-бэкенд, LLM, генерация картинок, статика на S3+CDN, Tailwind. Всё это общедоступно.
- «Секрет» VERSTKA.ai — **не технологии, а продукт**: дизайн-система + библиотека секций + месяцы шлифовки промптов + встроенные сервисы (CRM, заявки, домены) в одной связке.
- Реалистичный путь: **Фаза 0 за 1–2 недели** на чистом API LLM покажет, сходится ли качество, затем MVP за 2–4 месяца и наращивание до полного продукта за 6–12 месяцев с командой из 3–5 человек.
